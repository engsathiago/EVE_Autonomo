[project]
name = "agent-core"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115",
  "uvicorn[standard]>=0.32",
  "pydantic>=2.9",
  "pydantic-settings[yaml]>=2.6",
  "asyncpg>=0.30",
  "redis>=5.2",
  "anthropic>=0.40",
  "httpx>=0.28",
  "structlog>=24.4",
  "pyyaml>=6.0",
  # Phase 2: memory
  "sentence-transformers>=2.7.0",
  "torch>=2.2.0",
  "pgvector>=0.3.0",
  # Phase 3: skills
  "python-frontmatter>=1.1",
  "jinja2>=3.1",
  # Phase 4: multi-model
  "openai>=1.50.0",
  "ollama>=0.4.0",
  # Phase 6: cron + subagents
  "apscheduler==3.10.4",
  "croniter==2.0.5",
  "sqlalchemy==2.0.36",
  "psycopg2-binary==2.9.9",
  # Phase 10: deploy
  "prometheus_client>=0.21",  # métricas Prometheus: 12 séries do §5.3
  "psutil>=6.1",              # RSS gauge (agent_memory_bytes)
  # Phase 12: canais extras
  "discord.py>=2.3.2",
  "slack-bolt>=1.18.0",
  "slack-sdk>=3.27.0",
  "aioimaplib>=1.0.1",
  "aiosmtplib>=3.0.1",
  "email-validator>=2.1.0",
]

[project.optional-dependencies]
dev = [
  "pytest>=8.3",
  "pytest-asyncio>=0.24",
  "pytest-mock>=3.14",
  "ruff>=0.8",
  "mypy>=1.13",
  "respx>=0.21",
]
# Dependências de fine-tuning — instalação separada para não impactar o core.
# Versões pinned: testadas com Qwen 2.5 7B + RTX 4070 Ti Super, mai/2026.
# unsloth>=2025.5: última release estável com suporte a Qwen 2.5 + 4-bit QLoRA.
# peft>=0.14: requerido pelo unsloth; inclui LoraConfig com target_modules automático.
# transformers>=4.46: suporte a Qwen 2.5 + chat template correto (<|im_start|>).
# bitsandbytes>=0.44: quantização 4-bit em GPU NVIDIA (bnb_4bit_compute_dtype=bfloat16).
# datasets>=3.0: API Dataset.from_list() estável; versões < 3.0 têm breaking changes.
# rouge-score>=0.1.2: ROUGE-L F1 para eixo summarization_quality (alternativa ao LCS interno).
finetune = [
  "unsloth>=2025.5",
  "peft>=0.14",
  "transformers>=4.46",
  "bitsandbytes>=0.44",
  "datasets>=3.0",
  "rouge-score>=0.1.2",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/agent"]

[tool.ruff]
line-length = 120
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]
ignore = [
  "E501",   # line too long — não enforçado neste projeto
  "F841",   # local variable assigned but never used — pre-existente
  "F821",   # undefined name — pre-existente em alguns módulos
  "E402",   # module level import not at top — pre-existente
  "E741",   # ambiguous variable name
  "E731",   # lambda assignment
  "UP040",  # type alias — Python 3.11 compat
  "UP042",  # StrEnum — Python 3.11 compat
  "UP045",  # X | None syntax
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
markers = [
  "integration: testes que requerem Postgres rodando (desabilitar com -m 'not integration')",
  "docker: testes que requerem Docker daemon disponível (desabilitar com -m 'not docker')",
  "slow: testes que demoram (GPU, treino real) — desabilitar com -m 'not slow'",
]

[tool.coverage.report]
fail_under = 30
show_missing = false
omit = ["*/tests/*", "*/migrations/*"]

[tool.mypy]
python_version = "3.11"
strict = true
